---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: login-server
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  values:
    controllers:
      auth:
        replicas: 1
        strategy: RollingUpdate
        annotations:
          reloader.stakater.com/auto: "true"

        containers:
          app:
            image:
              repository: ghcr.io/goffxi/login-server
              tag: v0.1.1
            args: >-
              --role=auth
            env:
              SERVER_PORT: &authPort "54231"
              AUTH_SERVER_TLS_CERT_PATH: "/app/tls.crt"
              AUTH_SERVER_TLS_KEY_PATH: "/app/tls.key"
            envFrom:
              - configMapRef:
                  name: "{{ .Release.Name }}-config"
            resources:
              requests:
                cpu: 5m
                memory: 128Mi
              limits:
                memory: 256Mi

    defaultPodOptions:
      hostUsers: false

    service:
      auth:
        type: LoadBalancer
        sessionAffinity: ClientIP
        annotations:
          lbipam.cilium.io/ips: "10.50.100.7"
        ports:
          tcp:
            port: *authPort

    persistence:
      tls-certs:
        type: secret
        name: "{{ .Release.Name }}-tls"
        advancedMounts:
          auth:
            app:
              - path: /app/tls.crt
                subPath: tls.crt
                readOnly: true
              - path: /app/tls.key
                subPath: tls.key
                readOnly: true
