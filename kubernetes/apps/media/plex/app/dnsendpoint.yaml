apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: plex
spec:
  endpoints:
  - dnsName: "plex.${DOMAIN_5}"
    recordTTL: 60
    recordType: CNAME
    targets:
    - "external.${DOMAIN_5}"
