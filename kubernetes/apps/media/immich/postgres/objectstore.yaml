---
apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: immich-r2
spec:
  configuration:
    data:
      compression: bzip2
    destinationPath: s3://batcave-immich-postgres/
    endpointURL: https://d09aa6ad2c849681b113b8dbb5cd3820.r2.cloudflarestorage.com
    s3Credentials:
      accessKeyId:
        name: immich-postgres-secret
        key: AWS_ACCESS_KEY_ID
      secretAccessKey:
        name: immich-postgres-secret
        key: AWS_SECRET_ACCESS_KEY
    wal:
      compression: bzip2
      maxParallel: 8
  retentionPolicy: 30d
