---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: downflate
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: downflate-secret
  data:
    - secretKey: DOWNFLATE_WEBHOOK_SECRET
      remoteRef:
        key: konflate
        property: WEBHOOK_SECRET
    - secretKey: DOWNFLATE_GITHUB_APP_CLIENT_ID
      remoteRef:
        key: konflate
        property: APP_CLIENT_ID
    - secretKey: DOWNFLATE_GITHUB_APP_PRIVATE_KEY
      remoteRef:
        key: konflate
        property: APP_PRIVATE_KEY
        decodingStrategy: Base64
