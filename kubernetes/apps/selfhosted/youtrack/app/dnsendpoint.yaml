---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: youtrack-internal
  labels:
    dns.homelab/external: "false"
spec:
  endpoints:
    - dnsName: "youtrack.${DOMAIN_7}"
      recordTTL: 60
      recordType: CNAME
      targets:
        - "internal.${DOMAIN_7}"
---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: youtrack-external
  labels:
    dns.homelab/external: "true"
spec:
  endpoints:
    - dnsName: "youtrack.${DOMAIN_7}"
      recordTTL: 60
      recordType: CNAME
      targets:
        - "external.${DOMAIN_7}"
