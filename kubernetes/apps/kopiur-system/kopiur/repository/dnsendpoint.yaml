---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: kopia
  labels:
    dns.homelab/external: "false"
spec:
  endpoints:
    - dnsName: "kopia.${DOMAIN_5}"
      recordTTL: 60
      recordType: CNAME
      targets:
        - "internal.${DOMAIN_5}"
