---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: kopia-storage
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: kopia-storage-secret
  dataFrom:
    - extract:
        key: kopia-storage
