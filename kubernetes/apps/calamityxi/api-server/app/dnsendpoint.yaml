---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: api-server
  labels:
    dns.homelab/external: "true"
spec:
  endpoints:
  - dnsName: "api.${DOMAIN_8}"
    recordTTL: 60
    recordType: CNAME
    targets:
    - "external.${DOMAIN_8}"
