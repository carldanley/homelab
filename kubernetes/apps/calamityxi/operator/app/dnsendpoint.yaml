---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: game-server
  labels:
    dns.homelab/external: "false"
spec:
  endpoints:
  - dnsName: "login.${DOMAIN_8}"
    recordTTL: 60
    recordType: A
    targets:
    - "10.50.100.30"
