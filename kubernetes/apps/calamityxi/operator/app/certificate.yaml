---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: game-server-connect
spec:
  commonName: "login.${DOMAIN_8}"
  dnsNames:
    - "login.${DOMAIN_8}"
  duration: 2160h
  isCA: false
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
    group: cert-manager.io
  renewBefore: 360h
  secretName: game-server-connect-tls
  usages:
    - server auth
