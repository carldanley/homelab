---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: cloudflared-tunnel-domain-5
spec:
  endpoints:
    - dnsName: "external.${DOMAIN_5}"
      recordType: CNAME
      targets:
      - "${TUNNEL_ID}.cfargotunnel.com"
