---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: domain-4-certificate
spec:
  secretName: domain-4-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: "${DOMAIN_4}"
  dnsNames:
    - "${DOMAIN_4}"
    - "*.${DOMAIN_4}"
