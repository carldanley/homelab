---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: bug-bounty-migrate
  labels:
    app.kubernetes.io/part-of: bug-bounty
automountServiceAccountToken: false
imagePullSecrets:
  - name: bug-bounty-registry
---
apiVersion: batch/v1
kind: Job
metadata:
  name: bug-bounty-migrate
  labels:
    app.kubernetes.io/name: bug-bounty-migrate
    app.kubernetes.io/part-of: bug-bounty
    app.kubernetes.io/version: "${BUG_BOUNTY_SERVER_VERSION}"
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        app.kubernetes.io/name: bug-bounty-migrate
        app.kubernetes.io/part-of: bug-bounty
        app.kubernetes.io/version: "${BUG_BOUNTY_SERVER_VERSION}"
        bug-bounty.unbound-xi.org/workload: migrate
    spec:
      restartPolicy: OnFailure
      serviceAccountName: bug-bounty-migrate
      automountServiceAccountToken: false
      enableServiceLinks: false
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: migrate
          image: git.r1p.dev/unbound-xi/bug-bounty-server:${BUG_BOUNTY_SERVER_VERSION}@${BUG_BOUNTY_SERVER_DIGEST}
          imagePullPolicy: IfNotPresent
          args:
            - migrate
          env:
            - name: BUG_BOUNTY_ENVIRONMENT
              value: test
            - name: BUG_BOUNTY_ADAPTER_MODE
              value: synthetic
            - name: BUG_BOUNTY_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: bug-bounty-migrator-database
                  key: BUG_BOUNTY_DATABASE_URL
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsGroup: 65532
            runAsNonRoot: true
            runAsUser: 65532
