---
# Game database for UnboundXI (uxi). Sized for the 10,000+ concurrent player
# design target with the database kept out of the tick path: the map servers
# hold live state in memory and write behind, so this cluster sees event-driven,
# batched writes only. The data set stays in the single-digit gigabytes for a
# long time; raise resources here (one line, rolling restart) when it grows.
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: uxi-postgres
spec:
  instances: 3
  imageName: ghcr.io/cloudnative-pg/postgresql:${POSTGRESQL_VERSION}
  primaryUpdateStrategy: unsupervised
  storage:
    # openebs-hostpath is a directory on each node's dedicated 2 TB NVMe. The
    # size is not enforced by the class, so the real ceiling is the drive.
    size: 100Gi
    storageClass: openebs-hostpath
  superuserSecret:
    name: uxi-postgres-secret
  enableSuperuserAccess: true
  bootstrap:
    initdb:
      database: uxi
      owner: uxi
      secret:
        name: uxi-postgres-app-secret
      dataChecksums: true
      postInitApplicationSQL:
        - CREATE EXTENSION IF NOT EXISTS pg_stat_statements;
  affinity:
    # Three instances on three distinct MS-A2 nodes. Local volumes pin each
    # instance to its node, so streaming replication is the HA.
    enablePodAntiAffinity: true
    topologyKey: kubernetes.io/hostname
    podAntiAffinityType: required
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
          - matchExpressions:
              - key: gpu.homelab/amd
                operator: In
                values: ["true"]
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          preference:
            matchExpressions:
              - key: kubernetes.io/hostname
                operator: In
                values: ["w02", "w03"]
  postgresql:
    shared_preload_libraries:
      - pg_stat_statements
    synchronous:
      # One synchronous standby: a committed economy transaction exists on two
      # nodes. Write-behind checkpoints opt out per transaction with
      # synchronous_commit = off. "preferred" keeps the primary writable if
      # no standby is available.
      method: any
      number: 1
      dataDurability: preferred
    parameters:
      max_connections: "200"
      shared_buffers: 3GB
      effective_cache_size: 8GB
      work_mem: 16MB
      maintenance_work_mem: 512MB
      wal_buffers: 64MB
      min_wal_size: 1GB
      max_wal_size: 8GB
      max_slot_wal_keep_size: 16GB
      checkpoint_timeout: 15min
      checkpoint_completion_target: "0.9"
      wal_compression: lz4
      archive_timeout: 60s
      random_page_cost: "1.1"
      effective_io_concurrency: "256"
      jit: "off"
      track_io_timing: "on"
      log_min_duration_statement: 100ms
      autovacuum_naptime: 15s
      autovacuum_vacuum_scale_factor: "0.05"
      autovacuum_analyze_scale_factor: "0.02"
      pg_stat_statements.track: top
  resources:
    requests:
      cpu: "4"
      memory: 8Gi
    limits:
      memory: 8Gi
      hugepages-2Mi: 4Gi # Requires vm.nr_hugepages >= 3072 on the host (talos/common.yaml.j2)
  monitoring:
    enablePodMonitor: false # see podmonitor.yaml
  managed:
    services:
      additional:
        # LAN access for trusted clients on the home network (the owner's
        # laptop, admin tooling). Cilium announces the address over BGP and
        # external-dns publishes the name in UniFi DNS. Auth is scram-sha-256
        # over the cluster's TLS; nothing here is reachable from the internet.
        - selectorType: rw
          updateStrategy: patch
          serviceTemplate:
            metadata:
              name: uxi-postgres-lan
              labels:
                # Internal only: external-dns-cloudflare publishes solely
                # resources labeled dns.homelab/external=true, so this name
                # exists in UniFi's LAN DNS and nowhere else.
                dns.homelab/external: "false"
              annotations:
                lbipam.cilium.io/ips: 10.50.100.6
                external-dns.alpha.kubernetes.io/hostname: postgres.${DOMAIN_9}
            spec:
              type: LoadBalancer
  plugins:
    - name: barman-cloud.cloudnative-pg.io
      isWALArchiver: true
      parameters:
        barmanObjectName: uxi-r2
        serverName: uxi-postgres-v1
