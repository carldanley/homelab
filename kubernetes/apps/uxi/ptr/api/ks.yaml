---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: uxi-ptr-api
spec:
  interval: 1h
  timeout: 10m
  path: ./kubernetes/apps/uxi/ptr/api/app
  prune: true
  wait: true
  dependsOn:
    - name: uxi-ptr-postgres
      namespace: uxi-ptr
    - name: cilium-gateway
      namespace: kube-system
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: uxi-ptr
  postBuild:
    substituteFrom:
      - kind: Secret
        name: domains-secret
        optional: false
