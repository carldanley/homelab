---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: uxi-ptr-social-postgres
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: uxi-ptr-social-postgres
    template:
      data:
        social-url: >-
          postgres://uxi_ptr:{{ .UXI_PTR_POSTGRES_PASS | urlquery | replace "+" "%20" }}@uxi-ptr-postgres-rw.uxi-ptr.svc.cluster.local:5432/uxi_ptr?sslmode=verify-full&sslrootcert=/postgres-ca/ca.crt&options=-csearch_path%3Dsocial
  dataFrom:
    - extract:
        key: uxi-ptr-postgres
